# Track 2 — From-Scratch GPT: Qualitative Samples

> **Run:** base_highlr  |  **Checkpoint:** best_val  |  **Params:** n_layer=6 n_embd=192 n_head=4 vocab_size=1024
> **Generated:** 2026-08-16T19:16:33.395175+00:00
> **max_new_tokens:** 100  |  **Sampling:** T=0.8, top_p=0.9  |  **Greedy:** T=0 (argmax)

> **Annotation key:** `memorization` — verbatim domain phrases; `incoherence` — repetition / degenerate output; `novel-plausible` — grammatical, on-topic, non-verbatim.

> **Note:** Auto-annotations are heuristics. Revise labels below after visual inspection of the completions against the training text.

---

## Sample 1

**Prompt:** `The transformer architecture relies on the attention mechanism to`

### Sampling (T=0.8, top_p=0.9)

```
The transformer architecture relies on the attention mechanism to stability or (INFECRARAC [6]).
OPRRRAG [18] ORRAGPT-bugging is not y of GPT-4]
AARAgate hard
blearning, EDGARRCAD [6] are refaMAgent k/Enthetice analytics, few-style prompt
```

> **Annotation:** novel-plausible

### Greedy (argmax)

```
The transformer architecture relies on the attention mechanism to stability.
Tritectors requirement of the fires and
s.
Table justness:
Table es the fressions.
Table y of LLM-based log analysis is not y of log analysis is not monently, and
redilying, and instead of frequirelated, and limitation, and
matency
```

> **Annotation:** novel-plausible

---

## Sample 2

**Prompt:** `In the context of language model evaluation, bits-per-byte measures`

### Sampling (T=0.8, top_p=0.9)

```
In the context of language model evaluation, bits-per-byte measures. InProceedings of the 3437, Weibration of Evication, 2023. URL https://com/abs/[3] Fr. Fox And Karl. Cop Secura
h Pon Domain, and S. Bel Rett: Association for Computational Leuu. IEEE/seean.org/abs/10.0/. URL https://www
```

> **Annotation:** novel-plausible

### Greedy (argmax)

```
In the context of language model evaluation, bits-per-byte measures and
intermodels.AD [20] Open-tuning
Software
STable knowledge for log analysis.AScentric, and requentrative extraction, and instruction-augmentedge.Achecentric
Table knowledge. [20] fine-based log analysis
malling
Composive framed
```

> **Annotation:** novel-plausible

---

## Sample 3

**Prompt:** `Fine-tuning a pre-trained model on a domain-specific dataset allows`

### Sampling (T=0.8, top_p=0.9)

```
Fine-tuning a pre-trained model on a domain-specific dataset allows with modeling and
understyle instruction. Instead of the reliability by systematic focation, the latasizing the normal cross-to-specific model.
s the modeling by a[7thouthavaxishavior assior: expars, 9threshot colvarilying the fred templates, and deployment of LLM4Log
```

> **Annotation:** novel-plausible

### Greedy (argmax)

```
Fine-tuning a pre-trained model on a domain-specific dataset allows. APIN 52.3.3.3.
[5] Assocuanalysis Downstream log parsing: ACM on large language models for Computinguistics, 2025. InProceedings of the 32th International Conference on Software Engineering (IN), pages 2–3–3. doi: 10 .1109/ICSE, 2025.1109/ICSEMNININININININI
```

> **Annotation:** novel-plausible

---

## Sample 4

**Prompt:** `The key advantage of training a model from scratch is`

### Sampling (T=0.8, top_p=0.9)

```
The key advantage of training a model from scratch is not joits [6, 8].
dated,

7]. Input Javior assework long famikethns, the frames, what models, which a sometimize automated
s can relevant formond tradiguration, but these approaches diffice that add generation and exairstandid drift, and controled [
```

> **Annotation:** novel-plausible

### Greedy (argmax)

```
The key advantage of training a model from scratch is
s.
Thnitigets, and lines toward to a smatency and requirelated, and remats [20, 92, 9].
Theminally requiressions are brittly fringly filters [20, ybncking and
bration, especially requirel
```

> **Annotation:** novel-plausible

---

## Sample 5

**Prompt:** `When the training loss decreases but validation loss increases, this indicates`

### Sampling (T=0.8, top_p=0.9)

```
When the training loss decreases but validation loss increases, this indicates to log summarization.F1, PAGG, FGA, F1 - - Template the model to the overheadability, the
the addownstream tasks from a sequence-style (emps,
dats, tradendecting with datGPT. APA, but also troducing is style logs in
posit”, sensitive (ivers, and nat
```

> **Annotation:** novel-plausible

### Greedy (argmax)

```
When the training loss decreases but validation loss increases, this indicates, and
s, and requentric, and
s.AIDs, 219, 214, 219, 219, 214, 219, 219].
Scashra, and Eventical, and Ev,
Screashrade,
Scentridentrade, and Section, and Evenue-tun
```

> **Annotation:** novel-plausible

---

## Sample 6

**Prompt:** `Parameter-efficient fine-tuning methods such as LoRA reduce the number of`

### Sampling (T=0.8, top_p=0.9)

```
Parameter-efficient fine-tuning methods such as LoRA reduce the number of
(to-s: iter-cisting).
Instruct the
4Log is the lines a raw orgeneration-so instead of theirst narning catas benchmark integrated by the about style sens of add to LLM to the model. Embigurrastive, augmentation that task-specific, the fambstructurriven by ten s
```

> **Annotation:** novel-plausible

### Greedy (argmax)

```
Parameter-efficient fine-tuning methods such as LoRA reduce the number of the
dated the
minement is the fine-style models: the frequent
mallenges the first
models and leveloperformation of the focused informance, and informance to the first
s and introading of this familying.
mallenges the frelaging of this frelated towor a f
```

> **Annotation:** novel-plausible

---

## Sample 7

**Prompt:** `The tokenizer plays a critical role in language modelling because`

### Sampling (T=0.8, top_p=0.9)

```
The tokenizer plays a critical role in language modelling because logs, such as makes, and coularrate firectly [20, 102, 9].
cluding settings are exparged to amits with heterpreadable, e [20]. AI in which extraction [20, 103]. Theirstyle
viewed as emo that support, 106].
while 111]. This ch
```

> **Annotation:** novel-plausible

### Greedy (argmax)

```
The tokenizer plays a critical role in language modelling because logs, and
directly [20, 18].
Tout the model can behavior a singlemallenges the model to the model can
ward
included instead of the software entice, and vocused to the model can behibritting the fine of the modeling, and
spars, and deployments [20, 18].
To
```

> **Annotation:** novel-plausible

---

## Sample 8

**Prompt:** `Compared to larger models, a small language model trained from scratch`

### Sampling (T=0.8, top_p=0.9)

```
Compared to larger models, a small language model trained from scratching. In practicly.
h signeds may behaviorlused into anoperator-style ins sometimes, logs assistent templates as preved with the operational naristing runtime logs, failure prediction, finals. In adapability and controllability, the fave as hard software and messages and
miting decompositing doms
```

> **Annotation:** novel-plausible

### Greedy (argmax)

```
Compared to larger models, a small language model trained from scratching. This mators, the most usefully
s a same time, the most usefulness and it is a same to the most usefulness and verance.
s to the most warly, the model can before challs with a same time, and verance to the most usefully as a singly.
s to the most usefully, the most usefully is not only a s
```

> **Annotation:** novel-plausible

---
